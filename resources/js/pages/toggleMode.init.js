function toggleMode() {

    // Carrega Loader por 1seg na troca de modo noturno
    $('.loader').show();
    setTimeout(function () {
        $('.loader').hide();
    }, 1000);
    // ================================================//

}